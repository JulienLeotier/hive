<!--
Merci pour la contribution ! Remplis les sections ci-dessous —
anything unclear can be fine-tuned in review.
-->

## Résumé

<!-- 1-3 phrases : ce que fait ce PR, pourquoi. -->

## Changements

<!-- Bullet list des changements fonctionnels visibles. -->

-
-
-

## Test plan

<!-- Cases cochées quand vérifié. Au minimum, `go test ./...` +
     `npm run check` doivent être verts. -->

- [ ] `go test ./...` passe en local
- [ ] `go build ./...` passe en local
- [ ] `cd web && npm run check` passe en local
- [ ] `cd web && npm test` passe en local (si le changement touche web/)
- [ ] J'ai testé le changement à la main dans le dashboard (si UI)
- [ ] La CI GitHub Actions est verte

## Risques / impact

<!-- Migration DB ? Changement d'API (breaking) ? Consommation tokens
     Claude supplémentaire ? Performance ? -->

## Captures (si UI)

<!-- Avant / après. -->
